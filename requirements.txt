google-adk          # Agent, Runner, DatabaseSessionService — sessões persistentes
python-telegram-bot # bot Telegram
python-dotenv       # carrega variáveis do arquivo .env
requests
google-cloud-storage>=2.0
google-cloud-aiplatform>=1.71  # Vertex AI RAG Engine (vertexai.rag) — corpus/ingestão e busca da Kurisu
google-genai>=0.3    # SDK unificado do Gemini — chamada one-shot do Tutor de Idiomas (spec 031)
tzdata              # base de fusos horários p/ zoneinfo (imagens slim não trazem)
mcp                 # Model Context Protocol para integração com servidores MCP
google-auth         # Autenticação Google (OAuth2)
google-auth-oauthlib # OAuth2 para Google (flow de autorização)
google-api-python-client # Cliente Python para APIs Google (Calendar, Drive, etc.)
asyncpg             # driver async para PostgreSQL (exigido pelo DatabaseSessionService do ADK)
sqlalchemy          # ORM usado internamente pelo ADK para criar e acessar as tabelas
psycopg2-binary     # driver síncrono para PostgreSQL (usado pelas tools do journal)
python-dateutil     # aritmética de recorrência RRULE (RFC 5545) — motor da Kaguya (Fase 2)

# Web interface (webapp/)
fastapi              # Framework web ASGI — define rotas e serialização JSON
uvicorn[standard]    # Servidor ASGI que roda a aplicação FastAPI
authlib              # Implementa o fluxo Google OAuth (OIDC) para autenticação
itsdangerous         # Assina e verifica cookies de sessão de forma segura
python-multipart     # Suporte a formulários HTML no FastAPI (necessário para OAuth)
httpx                # Cliente HTTP async usado pelo Authlib no fluxo OAuth
