# ROADMAP — makima_personal_agent

> **Fonte única da verdade** para o status das fases do projeto. O `README.md` e o
> `CLAUDE.md` apontam para cá — ao entregar uma fase nova, atualize **apenas este arquivo**
> (e a documentação técnica afetada), não as tabelas de lá.

**Legenda:** ✅ entregue · 🔧 parcial (pendência aberta) · ⏳ planejado

## Fases

| Fase | O que é | Status |
|---|---|---|
| 1 | Nami (finanças): tools PostgreSQL + agente | ✅ |
| 2 | Kaguya (tarefas + agenda): motor próprio em PostgreSQL + Calendar via MCP + cross-agent atômico Kaguya+Nami. TickTick aposentado (spec 011) | ✅ |
| 012 | Kaguya — recorrência via RRULE (motor próprio `recurrence.py`) | ✅ |
| 013 | Kaguya — tags N:N, smart-lists (DSL) e calendário | ✅ |
| 014 (tasks) | Kaguya — hábitos com check-ins, heatmap e "força" (EMA) | ✅ |
| 3 / 027 | Kurisu (knowledge base): Vertex AI RAG sobre o vault Obsidian — corpus no ar (410/410 páginas), agente ativo no Telegram | ✅ |
| 4 | Lucy (email): tools Gmail + agente | ✅ |
| 5a | Frieren (livros): PostgreSQL + Google Books API + log de leitura + estantes | ✅ |
| — | Webapp (FastAPI + React) + diário Violet na web | ✅ |
| 014 | Komi (pessoas): identidade canônica + vínculos cross-agent + REST `/api/people/*` + shell `/people/*` | ✅ |
| 015 | Akane (filmes): PostgreSQL + TMDB v3 + Letterboxd + shell `/movies/*` | ✅ |
| 016 | Kaguya — Meu Dia + time-blocking (capacity bar, blocos de tempo) | ✅ |
| 017 | Kaguya — Matriz de Eisenhower (drag-and-drop 2×2) | ✅ |
| 018 | Kaguya — Command Palette ⌘K + atalhos + recorrência no quick-add | ✅ |
| 019 | Kaguya — Calendar Hub (fan-out multi-fonte: tarefas + Nami + Frieren + GCal) | ✅ |
| 020 | Kaguya — projetos/grupos/colunas (rework de listas) | ✅ |
| 021 | Marin (animes): PostgreSQL + Jikan/AniList/ARM + MAL OAuth2 PKCE + shell `/animes/*` | ✅ |
| 022 | Mai (séries de TV): PostgreSQL + TMDB API v3 + shell `/series/*` | ✅ |
| 023 | Makima Hub: página inicial agregadora (`/api/hub/summary` + MakimaShell) | ✅ |
| 024 | Kaguya — Kanban "Vidro" + views configuráveis (`kanban_views`) | ✅ |
| 025 | Kaguya — rework da lista de tarefas (árvore por grupo + board de grupo) | ✅ |
| 026 | Sync de aniversários Komi ↔ Kaguya (datas importantes viram tarefas recorrentes) | ✅ |
| 028 | Kurisu — memória unificada (multi-corpus + exporters + sync incremental) | 🔧 parcial |
| 029 | Kaguya — Tiny Experiments (mini-experimentos com log, pausa, revisão e aderência) | ✅ |
| 030 | Kaguya — Metas (áreas da vida, marcos, vínculos, progresso) | ✅ |
| 031 | Violet — Tutor de Idiomas (persona Kurisu): análise de escrita via Gemini, toggle original/corrigido, maestria por conceito (EMA), nível CEFR estimado, guia de estudo direcionável | ✅ |
| 032 | Scheduler de jobs recorrentes (`scheduler/`): APScheduler num container dedicado (`makima-scheduler`), registro declarativo de jobs (cron/intervalo, fuso São Paulo), log em `scheduler_runs` + alerta no Telegram em falha. Consolida backup + sync-Kurisu (loops aposentados) | ✅ |
| 032 (lucy) | Lucy (email/Gmail): agente interativo somente-leitura (IMAP) + digest matinal agendado (classificação Gemini + labels/arquivamento + Telegram) + histórico idempotente (`lucy_emails`). Aposenta o script externo do n8n | ✅ |
| 033 | **Frieren — reforma da webapp de livros** (só webapp, sem bot): (a) **página do livro editável** — modal "Editar livro" com todos os campos (capa por URL, título, autor, gênero, ano, páginas, ISBN, idioma, descrição, status, nota, datas, resenha, loja, preço) via `PATCH /metadata` estendido (+rating/datas/store_url/price); (b) **marcações coloridas** — seção "Minhas marcações" com bullets rosa/amarelo/verde/azul/laranja + página opcional (tabela `book_bullets` + tools/rotas); (c) **resenha inline** — editor estilo Violet na seção "Sua resenha" (escreve a qualquer momento; campo `notes`); (d) **gerenciamento de estantes** — criar/editar/excluir estante (`ShelfModal`) + adicionar/remover livros pela UI (backend já existia, faltava a UI); (e) **Biblioteca unificada** — agrupada por status (Lendo→Quero ler→Wishlist→Lidos), filtro lembrado + ordenação na toolbar (`created_at` exposto no `GET /api/books`), aba "Lendo agora" removida; (f) **heatmap** — meses em linha única com scroll (padrão Violet) cobrindo o ano inteiro (Frieren + Violet) | ✅ |
| 034 | **Kaguya — GTD core**: status GTD real (próxima ação/aguardando/algum dia — aposenta as tags heurísticas `#aguardando`/`#algum-dia`), contextos de execução dedicados (`task_contexts`, CRUD), processamento guiado do inbox (`process_inbox_item`, 6 decisões, web + Telegram `/processar_inbox`) e bloco fixo de views padrão de mercado (Todas/Hoje/Amanhã/Próximos 7 Dias/Inbox) | ✅ |
| 035 | **Kaguya — revisão semanal guiada**: wizard de 6 passos (inbox zero, próximas ações, aguardando, listas/projetos, calendário, algum dia/talvez) reusando a lógica da 034, registro leve (`task_weekly_reviews`) com retomada de revisão aberta (no máximo uma por vez, garantida por índice único parcial), lembrete no Telegram todo domingo 20h quando a semana termina sem revisão concluída (job `weekly_review_reminder` no scheduler) e indicador "última revisão há N dias" no painel. Webapp-only (sem tool ADK) | ✅ |
| 061 | **Violet — Conselho do Dia** (persona Violet, lógica na Kurisu): seção no topo da tela Escrever que, sob demanda, lê o dia (bullets + registros emocionais + cartas) + janela de 7 dias + tarefas/hábitos da Kaguya + os 3 conselhos anteriores, cruza com o RAG da Kurisu (`buscar_na_base`) e devolve 4 blocos (espelho do dia, ferramentas da base com citação real, pergunta de reflexão, ações — podem virar tarefa). Busca web só como complemento quando a base não cobre; origem ("base"/"web") decidida no servidor por checagem de `uri`, nunca por auto-declaração do modelo. Uma análise por dia (`journal_counsel`, `UNIQUE page_id`) | ✅ |
| 036 | **Kaguya — Metas/Hábitos cross-agent**: meta com **movimentos externos** vinculados manualmente (fase 1: livros da Frieren) e métrica em modo **automático** (calculada na leitura a partir do estado atual dos vínculos — nunca persistida; bloqueia edição manual nesse modo); hábito com **fonte automática** de check-in (diário da Violet para binário, leitura da Frieren somando páginas/dia para mensurável — nada persistido em `habit_checkins`, mesclado com os check-ins manuais na leitura). Mecanismo genérico por dois registries pequenos (`goal_link_providers.py`, `habit_source_providers.py`, espelhando o Calendar Hub) — um agente novo só publica um provedor no próprio pacote, sem mudar modelo/telas. Best-effort: falha de provedor degrada só o grupo afetado. Webapp-only (sem tool ADK) | ✅ frontend + backend (migração `goal_external_links`/`metric_mode`/`source_provider_id` pendente no VPS) |
| 040 | **Nami — correção de bugs (timezone, assinaturas, feedback de erro)**: toda derivação de "hoje" no domínio financeiro passa a usar America/Sao_Paulo (`_today_date()` com `ZoneInfo` no backend, `(NOW() AT TIME ZONE 'America/Sao_Paulo')::date` nas queries SQL diretas, `dateUtils.ts` com partes locais do navegador no frontend) — elimina contagem errada de parcelas/gastos perto da meia-noite; `get_spending_summary` fecha o `GROUP BY` num dict de colunas válidas; `create_subscription` valida `next_billing` (AAAA-MM-DD) e resolve o pagador dinamicamente contra contas/cartões cadastrados, gravando `account_id`/`card_id` mutuamente exclusivos (migração idempotente já executada em produção); saudação do Dashboard via `/auth/me` (fim do nome hardcoded); toasts nos catches silenciosos de categorias (Dashboard/Transações/Orçamentos/modal de lançamento). | ✅ |
| 041 | **Nami — Parcelamentos (tela + cartão de crédito)**: shell `/nami#parcelamentos` — lista de compras parceladas ativas com progresso (X/N, valor da parcela, restante), drill-down com a linha do tempo das parcelas (mês corrente destacado), criação aceitando conta **ou** cartão de crédito (`create_installment` ganhou `card_id`, resolvido dinamicamente — antes só aceitava conta), cancelamento de parcelas futuras e exclusão total (ambos com confirmação); card "Compromissos futuros" dos próximos 3 meses; tela Cartões ganha "Parcelamentos ativos" por cartão com comprometimento mensal da fatura + navegação para o drill-down. Migração idempotente (`installment_groups.account_id`/`card_id`) já executada em produção. | ✅ |
| 042 | **Nami — Dashboard: health score, tendência e pagamento de fatura**: card "Saúde financeira" (score 0-100 + 4 dimensões via `GET /health`, reusa o `Donut` já existente); card "Tendência de gastos" (evolução mensal + projeção via `GET /trend`, novo `AreaTrend` — SVG próprio, sem lib externa); ambos com falha isolada (um card quebrado não derruba o Dashboard). Tela Cartões ganha "Registrar pagamento" (`POST /cards/{id}/payment`) e a barra de utilização do limite — hardcoded em 0% até aqui — passa a refletir `divida_atual`/`utilizacao_pct` de verdade. `GET /summary` documentado como uso exclusivo do agente (sem UI, redundante com `/stats`). Corrige de passagem o default de mês do `GET /health` (usava `date.today()` UTC — trocado por `_today_date()` fuso São Paulo). | ✅ |
| 043 | **Nami — QoL do webapp: edição, exportação, filtros, transferências**: edição de transação reaproveitando o `AddModal` em modo dual (`PATCH /transactions/{id}` ganha `card_id`; corrigido de passagem: `update_transaction` nunca atualizava `account_id`/`card_id`, só o campo display `conta`). Edição de contas/cartões/assinaturas expondo `update_account`/`update_credit_card` (já existiam sem endpoint HTTP) via `PATCH /accounts/{id}`/`PATCH /cards/{id}`, mais campos visuais no `PATCH /subscriptions/{id}`. Exportação CSV do extrato (`GET /transactions/export`, `;` + BOM UTF-8) e filtro de categorias completo + persistência de filtro/ordenação em `localStorage`. Transferências entre contas — `create_transfer` grava par atômico `tipo='Transferencia'` via `get_conn()`, excluído de receita/despesa nos relatórios como consequência do filtro `tipo IN ('Despesa','Receita')` já existente (sem mudar esse código). Dashboard ganha heatmap mensal de gastos (`HeatmapMonth`, usa `stats.daily_spending` já existente). Paginação de `/transactions` (`limit`/`offset`/`has_more`) com "Carregar mais". Migração idempotente (`transactions.transfer_id`) já executada em produção. | ✅ |
| 044 | **Nami — Contas Fixas (separadas de Assinaturas, com confirmação de valor)**: reaproveita `subscriptions` (`kind='assinatura'\|'conta_fixa'`, `auto_lancar`) em vez de criar tabela nova — mesma mecânica de recorrência, comportamento diferente. Nova tela `/nami#contas-fixas` com status do ciclo corrente (paga/pendente/atrasada/agendada — `GET /recurring-status`, função pura `_cycle_status` que usa `next_billing_day` em vez de `next_billing`, já que este último é rolado para a frente assim que a conta é paga). "Marcar como paga" confirma o valor real (pode divergir do esperado) e roda atomicamente via `get_conn()` (`mark_subscription_paid`): cria a despesa vinculada **e** rola o vencimento — tudo ou nada; "Pular" rola sem lançar despesa. `Subscriptions.tsx` passa a filtrar só `kind='assinatura'` do mesmo array compartilhado do shell. Dashboard ganha "Custo fixo mensal" (contas fixas + assinaturas) com aviso de pendências. Agente Nami (Telegram) ganha `mark_subscription_paid`/`skip_subscription_cycle` e regra de classificação assinatura vs conta fixa. Migração idempotente (`subscriptions.kind`/`auto_lancar`) pendente no VPS. | ✅ frontend + backend (migração pendente no VPS) |
| 037 | **Kaguya — Foco/Pomodoro**: timer de foco no webapp, opcionalmente ligado a uma tarefa, com durações configuráveis (presets 25/5 e 50/10 + custom, lembrada em `focus_prefs`); widget flutuante (`FocusWidget`) persistente em todas as telas do painel, sobrevivendo a navegação e reload (tempo restante sempre derivado de `started_at`, nunca contado do zero na tela); sessão abandonada é fechada automaticamente na próxima leitura, creditando no máximo o tempo planejado (sem job/cron); resumo "Focado hoje" + série de 7 dias no Meu Dia. Webapp-only (sem tool ADK) | ✅ |
| 038 | **Kaguya — Meu Dia com contexto Trabalho/Pessoal**: contexto vive na **lista** (`task_projects.context`, Inbox sempre Pessoal — garantido por CHECK no schema) e no **calendário conectado** (`calendar_prefs.context`); tarefa herda o contexto da lista atual por JOIN (nunca uma coluna própria — muda de lista, muda de contexto na hora). Meu Dia ganha duas seções (Trabalho/Pessoal) com capacity própria — motor `compute_capacity` intocado, só chamado 2×/3× com insumos particionados (soma de `estimado_min`/`agenda_min`/`no_plano` bate com o total; `livre_min`/`folga_min` são independentes por design, cada barra usa a janela cheia). Ação em massa por grupo (`set_group_context`); toggle visão dividida/única em `localStorage` (US3); resumo do Telegram com os dois blocos. Webapp-only (sem tool ADK) | ✅ |
| 039 | **Kaguya — QoL: arquivar listas + localização nos eventos**: arquivar (`archive_project`/`restore_project`/`list_archived_projects`) reusa `task_projects.archived_at` (já existia, gravado internamente por `delete_project`) sem tocar tarefas/colunas — diferente de excluir; todas as views operacionais (Hoje, Meu Dia, matriz, calendário, smart-lists, tags) passam a excluir listas arquivadas, com a busca global como única exceção (sinaliza `archived: bool`); botão de arquivar no hover da sidebar (1 clique, sem confirmação) + tela "Arquivadas" com restaurar; Telegram avisa e oferece restaurar quando o usuário cita uma lista arquivada pelo nome. Local do evento (Google Calendar) chega ao Meu Dia (faltava só esse elo) e vira link clicável para o Google Maps nas 3 superfícies (agenda, popover, Meu Dia), sem chave de API. Nenhuma migração de schema. Webapp-only (sem tool ADK) | ✅ |
| 049 | **Akane — correção de 10 bugs** (backend, sync Letterboxd, webapp): `get_home` corrigido (`d.liked`→`m.liked`, evitava erro 500 na Início e no resumo do Telegram); sparkline de 7 dias passa a excluir filmes soft-deletados; Rewind mostra o nome de exibição de pessoas (`p.name`) em vez do normalizado de busca; paleta de pôster passa a `hashlib.md5` (determinística entre reinícios, `hash()` nativo era salgado por processo); histograma de notas na Início/Rewind corrigido (chave `"N.0"` casando com o formato já produzido pelo backend — inteiras não apareciam); busca do "Logar filme" (`search_movie`) agora sinaliza `local_id`/`in_catalog`, permitindo registrar reassistida de um filme já catalogado em vez de falhar; campo "assistido hoje" usa `todayLocalISO()` (fuso America/Sao_Paulo) em vez de `toISOString()` (UTC); sync RSS do Letterboxd passa a validar/clampar a nota (reusa `_parse_rating` do import CSV), a usar `pubDate` (RFC-822, via `email.utils.parsedate_to_datetime`) como fallback de data quando falta `watchedDate`, e a levantar `LetterboxdFetchError` em indisponibilidade real do feed (distinto de "zero itens novos") — aciona o alerta já existente do scheduler. Nenhuma migração de schema. | ✅ |
| 051 | **Akane — expõe na UI funcionalidades já prontas no backend** (webapp-only, sem tools/rotas novas): "+ Adicionar a lista" no detalhe do filme (`AddToListModal`, idempotente) + edição de nome/descrição/cor de lista existente; Cofre editável (adicionar/remover item sem sair do detalhe); botão de sincronização manual com o Letterboxd na sidebar (toast com o resumo ou erro); mapa de calor de atividade do ano no Rewind (`components/Heatmap.tsx`, porta de `frieren/ui/Heatmap.tsx`); exclusão de filme e de sessão do diário, ambas com confirmação em 2 etapas; bloco "Pessoas mais assistidas" no Rewind (dado já fluía desde a 049, só faltava renderizar); Início/Etiquetas/Listas distinguem erro de rede de "sem dados ainda" (aviso + retry); copy do estado vazio da Watchlist corrigida (referenciava um botão inexistente). Nenhuma migração de schema. | ✅ |
| 050 | **Akane — carga histórica do Letterboxd e correção de dados**: `watched.csv` passa a ser processado (fallback final — filme entra só com `status='watched'`, sem sessão, por falta de data confiável); `--no-tmdb` corrigido para se aplicar também à watchlist (`add_movie` ganhou parâmetro `enrich_tmdb`, antes inexistente); deduplicação de filme deixou de ser só `letterboxd_uri`/`tmdb_id` informado — `_resolve_movie_identity()` resolve por tmdb_id (informado ou enriquecido) e, por último, título normalizado+ano, **fundindo** no registro existente em vez de duplicar (cadastro manual + import/sync do mesmo filme = 1 registro só); metadados TMDB passam a `en-US` (`_TMDB_LANG`); botão "Buscar Dados" no detalhe do filme (`refresh_movie_metadata` + rota `POST /{id}/refresh-metadata`) rebusca e sobrescreve só os campos de catálogo, com fluxo de "Trocar filme" (`TmdbCandidatesModal`) para corrigir um match errado — nunca toca em dado pessoal; edição manual de filme (`EditMovieModal` + `PATCH /{id}/catalog`) e de sessão do diário (form inline em `MovieDetailScreen` + `PATCH /diary/{id}`, recalcula `last_watched_date`/`times_watched`); ordem de sessões no mesmo dia agora é contratual (`created_at` incremental na importação, não mais implícito) e reordenável na tela Diário (setas ↑/↓ por dia, `PATCH /diary/reorder`). Nenhuma migração de schema. | ✅ |

## Status atual (jul/2026)

Fases 001–027, 029, 030, 031, 032, 032 (lucy), 033, 034, 035, 036, 037, 038, 039, 049, 050,
051 e 061 entregues. A **028** (memória unificada da Kurisu) está parcial:
fundação (`agents/kurisu/memory/`) + sync incremental + 2 dos 8 exporters (diário, tarefas)
prontos localmente; deploy no VPS e os 6 exporters restantes pendentes.

## Pendências em aberto

| Pendência | Onde está o plano |
|---|---|
| Concluir a 028 (6 exporters + deploy VPS) | `specs/028-kurisu-unified-memory/` |
| Personalidade Violet + rename `agents/journal → agents/violet` | `docs/planos/PLANO_VIOLET_EVERGARDEN.md` |
| Integração Violet ↔ Komi (autocomplete `@menção` no diário) | `docs/planos/PLANO_INTEGRACAO_VIOLET_KOMI.md` |
| Aplicar a migração da 036 no VPS (schema + verificar os 5 cenários de `quickstart.md` contra Postgres real) | `specs/036-goal-habit-links/quickstart.md` |
| Aplicar a migração da 038 no VPS (`task_projects.context`/`calendar_prefs.context` + verificar os cenários de `quickstart.md` contra Postgres real) | `specs/038-meudia-work-context/quickstart.md` |
| Verificar os cenários de `quickstart.md` da 039 contra Postgres real (sem migração — só reusa `archived_at` já existente) | `specs/039-tasks-qol/quickstart.md` |
| Verificar os cenários de `quickstart.md` da 049 contra Postgres real (sem migração — 10 correções de bug da Akane) | `specs/049-akane-bugfixes/quickstart.md` |
| Verificar os cenários de `quickstart.md` da 051 contra Postgres real (sem migração — 8 gaps de UI da Akane) | `specs/051-akane-ui-gaps/quickstart.md` |
| Verificar os cenários de `quickstart.md` da 050 contra Postgres real (sem migração) e, depois, executar a carga real do export do usuário no VPS | `specs/050-akane-letterboxd-carga/quickstart.md` |
