---
wave: 3
depends_on: [026-PLAN-02]
files_modified:
  - agents/komi/tools.py
  - agents/kaguya/tools_tasks.py
  - agents/komi/calendar_provider.py
  - webapp/backend/routers/pessoas.py
autonomous: true
requirements_addressed: [REQ-A2, REQ-A3, REQ-A4, REQ-B5, REQ-B6, REQ-B7, REQ-C8, REQ-C9, REQ-C10, REQ-D11]
---

# 026-PLAN-03 — Wave 3: Hooks, Dedup e REST

Esta wave conecta tudo que foi criado na Wave 2:
(a) hooks best-effort nos 3 mutadores de Komi (add/update/delete de person_date);
(b) hooks best-effort nos 3 mutadores de Kaguya (create/update/delete de task) para type=birthday;
(c) deduplicação na agenda (calendar_provider.py omite person_dates com link em birthday_sync_links);
(d) endpoints REST PATCH e DELETE para datas + campo is_synced no GET.

---

<task id="T-03-A">
  <title>Hook Komi→Kaguya em add_important_date</title>
  <action>
    Em agents/komi/tools.py, na função add_important_date, após o bloco with get_conn()
    (após o commit implícito do context manager), adicionar bloco try/except best-effort:
      try:
          from agents.kaguya import komi_sync as _ks
          _ks.push_person_date(date_id)
      except Exception:
          pass
    Comentário: "Hook best-effort — propaga a data de aniversário criada para a Kaguya.
    Fica FORA do bloco with get_conn() para não abortar o CRUD principal se o sync falhar.
    O date_id foi obtido pelo RETURNING id (Wave 2)."
    O hook deve ficar FORA do bloco with get_conn() — mesma posição que gcal_sync em tools_tasks.py.
  </action>
  <read_first>
    - agents/komi/tools.py (add_important_date com RETURNING id da Wave 2)
    - agents/kaguya/tools_tasks.py (hook gcal_sync.push_task — padrão exato de posicionamento e try/except)
    - agents/kaguya/komi_sync.py (push_person_date — criado na Wave 2)
  </read_first>
  <acceptance_criteria>
    - [ ] O bloco try/except com _ks.push_person_date(date_id) existe após o with get_conn() em add_important_date
    - [ ] Está FORA do bloco with get_conn() (não dentro da transação)
    - [ ] Criar person_date com label "aniversário" via add_important_date resulta em 1 task type=birthday no banco
    - [ ] Criar person_date com label "formatura" via add_important_date NÃO cria tarefa (não contém 'anivers')
    - [ ] KOMI_SYNC_ENABLED=false: add_important_date funciona normalmente sem criar tarefa
  </acceptance_criteria>
</task>

<task id="T-03-B">
  <title>Hook Komi→Kaguya em update_important_date</title>
  <action>
    Em agents/komi/tools.py, na função update_important_date (criada na Wave 2),
    após o bloco with get_conn(), adicionar hook best-effort:
      try:
          from agents.kaguya import komi_sync as _ks
          _ks.push_person_date(date_id)
      except Exception:
          pass
    Comentário: "Propaga a mudança para a tarefa Kaguya correspondente, se houver link.
    O komi_sync.push_person_date compara o valor atual com o salvo — no-op se idêntico (anti-loop)."
  </action>
  <read_first>
    - agents/komi/tools.py (update_important_date criada na Wave 2)
    - agents/kaguya/tools_tasks.py (hook gcal_sync em update_task — padrão de posicionamento)
  </read_first>
  <acceptance_criteria>
    - [ ] O bloco try/except com _ks.push_person_date(date_id) existe após o with get_conn() de update_important_date
    - [ ] Atualizar date de um person_date de aniversário com link: tasks.due_date muda para o novo valor
    - [ ] Atualizar date para o mesmo valor já existente: 0 writes extras (no-op por anti-loop)
    - [ ] KOMI_SYNC_ENABLED=false: update_important_date funciona sem propagar
  </acceptance_criteria>
</task>

<task id="T-03-C">
  <title>Hook Komi→Kaguya em delete_important_date</title>
  <action>
    Em agents/komi/tools.py, na função delete_important_date (criada na Wave 2):
    ANTES do bloco with get_conn(), buscar o task_id do link (se existir):
      link_rows = run_select(
          "SELECT task_id FROM birthday_sync_links WHERE person_date_id = %s", (date_id,)
      )
      linked_task_id = link_rows[0]["task_id"] if link_rows else None
    Depois do bloco with get_conn() (após o DELETE da person_date ter sido commitado e o cascade
    ter removido o link do birthday_sync_links):
      if linked_task_id:
          try:
              from agents.kaguya import komi_sync as _ks
              _ks.remove_person_date(linked_task_id)
          except Exception:
              pass
    Comentário: "O task_id é lido ANTES do delete porque o ON DELETE CASCADE remove o link junto
    com a person_date. Após o commit, o link já não existe — por isso lemos o task_id antes.
    remove_person_date recebe task_id (não date_id) para poder chamar delete_task."
  </action>
  <read_first>
    - agents/komi/tools.py (delete_important_date criada na Wave 2)
    - agents/kaguya/komi_sync.py (remove_person_date — confirmar que recebe task_id)
    - agents/kaguya/tools_tasks.py (hook gcal_sync em delete_task — padrão de posicionamento)
  </read_first>
  <acceptance_criteria>
    - [ ] SELECT task_id FROM birthday_sync_links ocorre ANTES do DELETE de person_date
    - [ ] O linked_task_id é passado a _ks.remove_person_date (não o date_id)
    - [ ] Deletar person_date com link: tarefa correspondente tem deleted_at IS NOT NULL
    - [ ] Deletar person_date SEM link (label "formatura"): nenhuma tarefa afetada
    - [ ] KOMI_SYNC_ENABLED=false: delete_important_date remove person_date sem chamar remove_person_date
  </acceptance_criteria>
</task>

<task id="T-03-D">
  <title>Hook Kaguya→Komi em create_task para type=birthday</title>
  <action>
    Em agents/kaguya/tools_tasks.py, na função create_task, após o bloco try/except do gcal_sync,
    adicionar hook condicional para type=birthday:
      # Propaga para Komi se for aniversário (best-effort, pós-commit)
      if type == "birthday":
          try:
              from agents.kaguya import komi_sync as _ks
              _ks.push_birthday(new_id)
          except Exception:
              pass
    Comentário: "Hook Kaguya→Komi: cria person_date na Komi quando uma tarefa de aniversário
    é criada. Lazy import dentro do try/except evita ciclo agents.kaguya → agents.komi → agents.kaguya."
    O type aqui é o valor da variável local (string "birthday") — não precisa query extra.
    O new_id é o id da tarefa recém-criada, disponível após o INSERT.
  </action>
  <read_first>
    - agents/kaguya/tools_tasks.py (create_task — onde está o gcal_sync.push_task, para ver posição exata)
    - agents/kaguya/komi_sync.py (push_birthday — criado na Wave 2)
  </read_first>
  <acceptance_criteria>
    - [ ] O bloco if type == "birthday" com _ks.push_birthday(new_id) existe após o gcal_sync hook em create_task
    - [ ] Criar task type=birthday com person_ids=["uuid..."] via create_task gera person_date "aniversário" na Komi + 1 linha em birthday_sync_links
    - [ ] Criar task type=task NÃO dispara push_birthday
    - [ ] Criar task type=birthday SEM person_ids: push_birthday é chamado mas retorna no-op (sem assignee válido)
    - [ ] KOMI_SYNC_ENABLED=false: create_task funciona sem criar person_date
  </acceptance_criteria>
</task>

<task id="T-03-E">
  <title>Hook Kaguya→Komi em update_task para type=birthday</title>
  <action>
    Em agents/kaguya/tools_tasks.py, na função update_task, após o bloco try/except do gcal_sync,
    adicionar hook que verifica o type atual da tarefa após o commit e propaga se for birthday:
      try:
          from agents.db import run_select as _rs
          _type_rows = _rs("SELECT type FROM tasks WHERE id = %(tid)s", {"tid": task_id})
          if _type_rows and _type_rows[0]["type"] == "birthday":
              from agents.kaguya import komi_sync as _ks
              _ks.push_birthday(task_id)
      except Exception:
          pass
    Comentário: "Propaga para Komi se a tarefa é (ou virou) aniversário após o update.
    O SELECT do type ocorre APÓS o commit para pegar o valor atualizado.
    O komi_sync.push_birthday compara due_date atual — no-op se já sincronizado (anti-loop)."
  </action>
  <read_first>
    - agents/kaguya/tools_tasks.py (update_task — gcal_sync hook, para ver posição exata)
    - agents/kaguya/komi_sync.py (push_birthday — anti-loop por convergência)
  </read_first>
  <acceptance_criteria>
    - [ ] O hook existe após o gcal_sync hook em update_task
    - [ ] Atualizar due_date de task type=birthday: person_dates.date muda para o novo valor
    - [ ] Atualizar due_date para o MESMO valor: 0 writes extras (anti-loop funciona)
    - [ ] Atualizar task type=task (não birthday): push_birthday não é chamado
    - [ ] KOMI_SYNC_ENABLED=false: update_task funciona sem propagar
  </acceptance_criteria>
</task>

<task id="T-03-F">
  <title>Hook Kaguya→Komi em delete_task scope=series para type=birthday</title>
  <action>
    Em agents/kaguya/tools_tasks.py, na função delete_task, após o bloco try/except do gcal_sync,
    adicionar hook condicional apenas para scope="series":
      if scope == "series":
          try:
              from agents.db import run_select as _rs
              _type_rows = _rs(
                  "SELECT type FROM tasks WHERE id = %(tid)s", {"tid": task_id}
              )
              if _type_rows and _type_rows[0]["type"] == "birthday":
                  from agents.kaguya import komi_sync as _ks
                  _ks.remove_birthday(task_id)
          except Exception:
              pass
    Comentário: "Remove o person_date correspondente na Komi somente quando scope='series'
    (encerra a série inteira). scope='this' apaga apenas a ocorrência atual — a série continua.
    A tarefa já está soft-deleted quando este hook roda (deleted_at IS NOT NULL), mas o
    birthday_sync_links ainda existe porque soft-delete não dispara ON DELETE CASCADE."
  </action>
  <read_first>
    - agents/kaguya/tools_tasks.py (delete_task completo — gcal_sync hook, para ver posição exata)
    - agents/kaguya/komi_sync.py (remove_birthday — criado na Wave 2)
  </read_first>
  <acceptance_criteria>
    - [ ] O hook existe após o gcal_sync hook em delete_task
    - [ ] delete_task(task_id, scope="series") com type=birthday: person_date correspondente é deletado e birthday_sync_links não contém o par
    - [ ] delete_task(task_id, scope="this") com type=birthday: person_date NÃO é deletado
    - [ ] delete_task com type=task: remove_birthday não é chamado
    - [ ] KOMI_SYNC_ENABLED=false: delete_task funciona sem deletar person_date
  </acceptance_criteria>
</task>

<task id="T-03-G">
  <title>Dedup agenda: calendar_provider.py omite person_dates com link</title>
  <action>
    Em agents/komi/calendar_provider.py, na função list_calendar_events (ou equivalente),
    modificar a query SQL que lista person_dates para adicionar LEFT JOIN com birthday_sync_links
    e filtrar com WHERE bsl.person_date_id IS NULL:
    Nova query deve incluir:
      FROM person_dates pd
      ...
      LEFT JOIN birthday_sync_links bsl ON bsl.person_date_id = pd.id
      WHERE ... AND bsl.person_date_id IS NULL
    (O filtro exclui person_dates que já têm correspondência em birthday_sync_links — esses
    são exibidos como tarefa Kaguya na agenda; exibi-los também aqui causaria duplicata.)
    Comentário abundante: "A agenda unificada exibiria o aniversário duplicado (person_date da Komi
    + task type=birthday da Kaguya). Filtramos os person_dates sincronizados para que o
    aniversário apareça apenas 1x — como a tarefa interativa da Kaguya. Datas sem link
    (label não-aniversário, ou aniversários ainda não sincronizados) continuam aparecendo."
  </action>
  <read_first>
    - agents/komi/calendar_provider.py (query atual de listagem de person_dates — ver toda a função)
    - agents/kaguya/schema_tasks_pg.sql (birthday_sync_links DDL da Wave 1, confirmar nome das colunas)
  </read_first>
  <acceptance_criteria>
    - [ ] A query tem LEFT JOIN birthday_sync_links bsl ON bsl.person_date_id = pd.id
    - [ ] A query tem WHERE ... AND bsl.person_date_id IS NULL (ou equivalente NOT EXISTS)
    - [ ] person_date COM link em birthday_sync_links: list_calendar_events não retorna esse item
    - [ ] person_date SEM link (label "formatura"): list_calendar_events continua retornando normalmente
    - [ ] person_date de aniversário SEM link (não sincronizado): ainda aparece na agenda
  </acceptance_criteria>
</task>

<task id="T-03-H">
  <title>REST: PATCH e DELETE para datas em pessoas.py</title>
  <action>
    Em webapp/backend/routers/pessoas.py:

    1. Importar as novas tools no bloco de imports existente:
       from agents.komi.tools import (
           ...,  # imports já existentes
           update_important_date,
           delete_important_date,
       )

    2. Adicionar modelo Pydantic UpdateImportantDateBody (campos todos Optional):
       class UpdateImportantDateBody(BaseModel):
           label: Optional[str] = None
           date: Optional[str] = None    # YYYY-MM-DD
           recurring: Optional[bool] = None

    3. Adicionar endpoint PATCH /{person_id}/dates/{date_id}:
       @router.patch("/{person_id}/dates/{date_id}")
       def update_person_date(
           person_id: str, date_id: int,
           body: UpdateImportantDateBody,
           user: dict = Depends(require_user),
       ):
           campos = {k: v for k, v in body.model_dump(exclude_unset=True).items() if v is not None}
           return _check_result(update_important_date(date_id, **campos))

    4. Adicionar endpoint DELETE /{person_id}/dates/{date_id} com status_code=204:
       @router.delete("/{person_id}/dates/{date_id}", status_code=204)
       def delete_person_date(
           person_id: str, date_id: int,
           user: dict = Depends(require_user),
       ):
           result = delete_important_date(date_id)
           if result.get("status") == "error":
               raise HTTPException(status_code=400, detail=result.get("message", "Erro ao deletar."))

    As novas rotas devem ser adicionadas APÓS o POST /{person_id}/dates existente.
    Comentários em português explicando o padrão model_dump(exclude_unset=True) para PATCH parcial.
  </action>
  <read_first>
    - webapp/backend/routers/pessoas.py (estrutura atual — especialmente POST /dates e o padrão _check_result)
    - agents/komi/tools.py (update_important_date e delete_important_date — assinaturas da Wave 2)
  </read_first>
  <acceptance_criteria>
    - [ ] PATCH /api/people/{person_id}/dates/{date_id} com body {"date": "2026-08-15"} retorna 200 e atualiza o banco
    - [ ] DELETE /api/people/{person_id}/dates/{date_id} retorna 204
    - [ ] PATCH com date_id inexistente retorna 400 com mensagem de erro
    - [ ] DELETE com date_id inexistente retorna 400
    - [ ] Ambas as rotas têm Depends(require_user)
    - [ ] UpdateImportantDateBody existe como modelo Pydantic com todos os campos Optional
    - [ ] DELETE retorna sem body (204 No Content)
  </acceptance_criteria>
</task>

<task id="T-03-I">
  <title>Campo is_synced no GET /api/people/{id} — get_person em tools.py</title>
  <action>
    Em agents/komi/tools.py, na função get_person, modificar a sub-query ou query que busca
    as datas (person_dates) da pessoa para incluir is_synced via LEFT JOIN com birthday_sync_links:
      SELECT pd.id, pd.label, pd.date::text, pd.recurring,
             CASE WHEN bsl.person_date_id IS NOT NULL THEN TRUE ELSE FALSE END AS is_synced
      FROM person_dates pd
      LEFT JOIN birthday_sync_links bsl ON bsl.person_date_id = pd.id
      WHERE pd.person_id = %(id)s
      ORDER BY pd.date
    O campo "id" já deve estar retornando (necessário para PATCH/DELETE) — se não estiver,
    adicionar também. O retorno de cada data passa a incluir is_synced: bool e id: int.
    Comentário: "is_synced=True indica que esta data tem correspondência em birthday_sync_links
    (tarefa Kaguya sincronizada). O frontend usa este campo para exibir o badge 🎂 sincronizado."
  </action>
  <read_first>
    - agents/komi/tools.py (get_person — localizar a query que busca person_dates)
    - agents/kaguya/schema_tasks_pg.sql (birthday_sync_links DDL da Wave 1)
  </read_first>
  <acceptance_criteria>
    - [ ] GET /api/people/{id} retorna "datas" com campo is_synced em cada item
    - [ ] GET /api/people/{id} retorna "datas" com campo id (inteiro) em cada item
    - [ ] person_date SEM link em birthday_sync_links: is_synced = false
    - [ ] person_date COM link em birthday_sync_links: is_synced = true
    - [ ] A mudança não quebra nenhuma query existente (todos os campos anteriores continuam presentes)
  </acceptance_criteria>
</task>

---

must_haves:
  goal: "Sincronização bidirecional automática de aniversários Komi ↔ Kaguya"
  truths:
    - "birthday_sync_links existe no banco"
    - "criar person_date de aniversário gera tarefa Kaguya"
    - "criar tarefa type=birthday gera person_date Komi"
    - "editar um lado reflete no outro (sem loop)"
    - "agenda mostra aniversário apenas 1x"
    - "KOMI_SYNC_ENABLED=false desliga sync sem quebrar CRUD"
