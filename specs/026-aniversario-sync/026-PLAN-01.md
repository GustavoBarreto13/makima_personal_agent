---
wave: 1
depends_on: []
files_modified:
  - agents/kaguya/schema_tasks_pg.sql
autonomous: true
requirements_addressed: [REQ-A1, REQ-B5]
---

# 026-PLAN-01 — Wave 1: Schema DDL

Adicionar as duas alterações de schema necessárias para a fase 026:
(a) tabela `birthday_sync_links` — ponte 1:1 entre `person_dates` e `tasks`;
(b) coluna `is_birthdays` em `task_projects` — identidade da lista "Aniversários".

Nenhum código Python ou TypeScript é tocado nesta wave. Apenas SQL idempotente
dentro do arquivo de schema que já existe.

---

<task id="T-01-A">
  <title>Adicionar tabela birthday_sync_links ao schema Kaguya</title>
  <action>
    Em agents/kaguya/schema_tasks_pg.sql, após a seção de `calendar_prefs` (antes do SEED),
    adicionar o bloco CREATE TABLE IF NOT EXISTS birthday_sync_links com:
    - Coluna person_date_id INT NOT NULL UNIQUE REFERENCES person_dates(id) ON DELETE CASCADE
    - Coluna task_id INT NOT NULL UNIQUE REFERENCES tasks(id) ON DELETE CASCADE
    - Coluna komi_label TEXT NOT NULL DEFAULT 'aniversário'
    - Coluna created_at TIMESTAMPTZ DEFAULT now()
    Adicionar também um índice: CREATE INDEX IF NOT EXISTS idx_bsl_task ON birthday_sync_links (task_id)
    para acelerar a consulta pelo lado task (komi_sync.push_birthday lê por task_id).
    O comentário de cabeçalho deve explicar que a tabela é a ponte 1:1 entre person_dates (Komi)
    e tasks type=birthday (Kaguya), criada na fase 026.
  </action>
  <read_first>
    - agents/kaguya/schema_tasks_pg.sql (estado atual — ver seção calendar_prefs e SEED no final)
    - agents/komi/schema_pg.sql (DDL de person_dates para confirmar que person_dates.id é SERIAL INT)
  </read_first>
  <acceptance_criteria>
    - [ ] O bloco CREATE TABLE IF NOT EXISTS birthday_sync_links existe no arquivo
    - [ ] As colunas person_date_id e task_id têm UNIQUE individualmente (não composto)
    - [ ] ON DELETE CASCADE está nas duas FKs
    - [ ] Coluna komi_label TEXT NOT NULL DEFAULT 'aniversário' existe
    - [ ] Índice idx_bsl_task existe no arquivo
    - [ ] O bloco está ANTES do primeiro INSERT INTO task_projects (SEED)
    - [ ] Executar o arquivo no banco não gera erro (idempotência — IF NOT EXISTS em toda criação)
  </acceptance_criteria>
</task>

<task id="T-01-B">
  <title>Adicionar coluna is_birthdays e índice único parcial em task_projects</title>
  <action>
    Em agents/kaguya/schema_tasks_pg.sql, fazer três mudanças:
    1. Dentro do CREATE TABLE IF NOT EXISTS task_projects (seção que define a tabela), adicionar
       a coluna is_birthdays BOOLEAN NOT NULL DEFAULT FALSE após a coluna is_inbox.
    2. Após o CREATE UNIQUE INDEX uq_task_projects_inbox, adicionar:
       CREATE UNIQUE INDEX IF NOT EXISTS uq_task_projects_birthdays
           ON task_projects (is_birthdays) WHERE is_birthdays;
       (garante exatamente 1 lista "Aniversários" no sistema, idêntico ao padrão do Inbox)
    3. Na seção "Migrações idempotentes", adicionar:
       ALTER TABLE task_projects ADD COLUMN IF NOT EXISTS is_birthdays BOOLEAN NOT NULL DEFAULT FALSE;
       (para bancos que já existem sem a coluna — padrão já seguido pela coluna google_event_id)
  </action>
  <read_first>
    - agents/kaguya/schema_tasks_pg.sql (linhas com is_inbox e seu índice; seção de ALTER TABLE de migração)
  </read_first>
  <acceptance_criteria>
    - [ ] Coluna is_birthdays BOOLEAN NOT NULL DEFAULT FALSE aparece no CREATE TABLE task_projects
    - [ ] CREATE UNIQUE INDEX uq_task_projects_birthdays com WHERE is_birthdays existe
    - [ ] ALTER TABLE task_projects ADD COLUMN IF NOT EXISTS is_birthdays existe na seção de migrações
    - [ ] Rodar o schema novamente em banco existente (sem a coluna) não gera erro
    - [ ] SELECT id FROM task_projects WHERE is_birthdays retorna 0 linhas (nenhum seed nesta wave — o seed é criado dinamicamente pelo helper _get_birthdays_list_id)
  </acceptance_criteria>
</task>

---

must_haves:
  goal: "Sincronização bidirecional automática de aniversários Komi ↔ Kaguya"
  truths:
    - "birthday_sync_links existe no banco"
    - "criar person_date de aniversário gera tarefa Kaguya"
    - "criar tarefa type=birthday gera person_date Komi"
    - "editar um lado reflete no outro (sem loop)"
    - "agenda mostra aniversário apenas 1x"
    - "KOMI_SYNC_ENABLED=false desliga sync sem quebrar CRUD"
