---
wave: 4
depends_on: [026-PLAN-03]
files_modified:
  - webapp/frontend/src/components/DatePicker.tsx
  - webapp/frontend/src/components/MiniCalendar.tsx
  - webapp/frontend/src/pages/kaguya/components/DatePicker.tsx
  - webapp/frontend/src/pages/kaguya/components/MiniCalendar.tsx
  - webapp/frontend/src/pages/komi/komiApi.ts
  - webapp/frontend/src/pages/komi/types.ts
  - webapp/frontend/src/pages/komi/screens/PersonPage.tsx
autonomous: false
requirements_addressed: [REQ-A4, REQ-D11, REQ-D12]
---

# 026-PLAN-04 — Wave 4: Frontend Komi

Esta wave implementa a UI de edição e exclusão de datas na ficha da pessoa (PersonPage),
incluindo:
(a) mover DatePicker e MiniCalendar para src/components/ (cross-shell);
(b) atualizar os arquivos originais da Kaguya para re-exportar do novo local;
(c) badge "🎂 sincronizado" para datas com is_synced=true;
(d) modal de edição de data com label + DatePicker + toggle recurring;
(e) botões ✏ e 🗑 em cada data na lista de dates da PersonPage.

`autonomous: false` porque a movimentação de arquivos e o UI exigem validação visual.

---

<task id="T-04-A">
  <title>Mover DatePicker e MiniCalendar para src/components/</title>
  <action>
    Criar dois novos arquivos em src/components/ com o conteúdo dos originais:

    1. webapp/frontend/src/components/MiniCalendar.tsx — conteúdo idêntico ao atual em
       pages/kaguya/components/MiniCalendar.tsx, ajustando apenas os imports internos
       para o novo caminho relativo (lib/dateUtils, ui/Icons, etc. ficam em paths relativos
       corretos a partir de src/components/).

    2. webapp/frontend/src/components/DatePicker.tsx — conteúdo idêntico ao atual em
       pages/kaguya/components/DatePicker.tsx, ajustando:
       - import de MiniCalendar: de './MiniCalendar' para './MiniCalendar' (mesmo nível)
       - imports de dateUtils, Icons e outros helpers: ajustar para path relativo a src/components/

    3. Modificar pages/kaguya/components/DatePicker.tsx para re-exportar do novo local
       (NÃO deletar o arquivo — mantém compatibilidade retroativa de todos os imports existentes):
       export { DatePicker } from '../../../components/DatePicker'
       Comentário: "Componente movido para src/components/ (fase 026) para uso cross-shell.
       Este arquivo existe apenas para compatibilidade retroativa dos imports na Kaguya."

    4. Modificar pages/kaguya/components/MiniCalendar.tsx para re-exportar do novo local:
       export { MiniCalendar } from '../../../components/MiniCalendar'
       Mesmo comentário de cabeçalho.

    Verificar se src/components/ já existe no projeto antes de criar arquivos.
    Rodar `npm run build` ou `npx tsc --noEmit` para confirmar zero erros de TypeScript.
  </action>
  <read_first>
    - webapp/frontend/src/pages/kaguya/components/DatePicker.tsx (conteúdo atual completo + todos os imports)
    - webapp/frontend/src/pages/kaguya/components/MiniCalendar.tsx (conteúdo atual completo + todos os imports)
    - webapp/frontend/src/components/ (verificar se existe e o que contém — ex.: Layout.tsx)
    - webapp/CLAUDE.md (§Inputs, data e hora — regras obrigatórias de DatePicker/TimePicker)
  </read_first>
  <acceptance_criteria>
    - [ ] Arquivo webapp/frontend/src/components/DatePicker.tsx existe com o conteúdo funcional
    - [ ] Arquivo webapp/frontend/src/components/MiniCalendar.tsx existe com o conteúdo funcional
    - [ ] pages/kaguya/components/DatePicker.tsx re-exporta de src/components/DatePicker.tsx
    - [ ] pages/kaguya/components/MiniCalendar.tsx re-exporta de src/components/MiniCalendar.tsx
    - [ ] O DatePicker continua funcionando no shell Kaguya (sem erro de compilação TypeScript)
    - [ ] Nenhum arquivo da Kaguya que importava DatePicker precisa ser alterado (re-export transparente)
  </acceptance_criteria>
</task>

<task id="T-04-B">
  <title>Atualizar types.ts da Komi: campo id e is_synced em ImportantDate</title>
  <action>
    Em webapp/frontend/src/pages/komi/types.ts, modificar a interface ImportantDate
    para adicionar os campos retornados pela Wave 3:
      export interface ImportantDate {
        id: number          // necessário para PATCH /dates/{date_id} e DELETE /dates/{date_id}
        label: string
        date: string        // YYYY-MM-DD
        recurring: boolean
        is_synced: boolean  // true = tem tarefa Kaguya correspondente em birthday_sync_links
      }
    Se importantDate era usado com o tipo Person.datas, garantir que Person.datas também
    esteja usando ImportantDate (não um tipo inline).
    Comentário explicando cada campo novo e de onde vem (GET /api/people/{id} — Wave 3).
  </action>
  <read_first>
    - webapp/frontend/src/pages/komi/types.ts (interface ImportantDate atual)
    - agents/komi/tools.py (get_person — confirmar que agora retorna id e is_synced por data, Wave 3)
  </read_first>
  <acceptance_criteria>
    - [ ] ImportantDate tem campo id: number
    - [ ] ImportantDate tem campo is_synced: boolean
    - [ ] Sem erros de compilação TypeScript nos componentes que usam ImportantDate
  </acceptance_criteria>
</task>

<task id="T-04-C">
  <title>Atualizar komiApi.ts: métodos updateDate e deleteDate</title>
  <action>
    Em webapp/frontend/src/pages/komi/komiApi.ts, adicionar no objeto komiApi
    (após o método addDate existente):

      /** Atualiza campos de uma data importante (PATCH parcial — fase 026). */
      updateDate: (personId: string, dateId: number, body: Partial<{
        label: string
        date: string
        recurring: boolean
      }>) =>
        api.patch<{ status: string }>(`/api/people/${personId}/dates/${dateId}`, body),

      /** Remove uma data importante (204 No Content — fase 026). */
      deleteDate: (personId: string, dateId: number) =>
        api.del<void>(`/api/people/${personId}/dates/${dateId}`),

    Comentários em português explicando que os endpoints foram criados na Wave 3 (fase 026).
    Verificar que api.patch e api.del existem em lib/api.ts — se api.del não existir, usar
    api.delete ou a forma correta conforme lib/api.ts.
  </action>
  <read_first>
    - webapp/frontend/src/pages/komi/komiApi.ts (addDate como referência de padrão)
    - webapp/frontend/src/lib/api.ts (verificar quais métodos existem: get, post, patch, del/delete, etc.)
  </read_first>
  <acceptance_criteria>
    - [ ] komiApi.updateDate existe e faz PATCH /api/people/{personId}/dates/{dateId}
    - [ ] komiApi.deleteDate existe e faz DELETE /api/people/{personId}/dates/{dateId}
    - [ ] Sem erros de compilação TypeScript
  </acceptance_criteria>
</task>

<task id="T-04-D">
  <title>Modal de edição de data e botões ✏/🗑 na PersonPage</title>
  <action>
    Em webapp/frontend/src/pages/komi/screens/PersonPage.tsx:

    1. Importar DatePicker do novo local cross-shell:
       import { DatePicker } from '../../../../components/DatePicker'

    2. Adicionar estado de UI para o modal de edição:
       const [editingDate, setEditingDate] = useState<ImportantDate | null>(null)
       const [dateFormLabel, setDateFormLabel] = useState('')
       const [dateFormDate, setDateFormDate] = useState('')
       const [dateFormRecurring, setDateFormRecurring] = useState(true)
       const [dateSaving, setDateSaving] = useState(false)

    3. Implementar handleEditDate(d: ImportantDate):
       Preenche os estados (dateFormLabel, dateFormDate, dateFormRecurring) com os valores de d,
       depois chama setEditingDate(d) para abrir o modal.

    4. Implementar handleDeleteDate(d: ImportantDate):
       Pede confirmação via window.confirm("Remover a data '{d.label}'?").
       Se confirmado: chama await komiApi.deleteDate(person.id, d.id), depois recarrega o perfil.
       Tratar erros com console.error.

    5. Implementar handleSaveDate():
       Chama await komiApi.updateDate(person.id, editingDate.id, {
         label: dateFormLabel,
         date: dateFormDate,
         recurring: dateFormRecurring,
       }).
       Fecha modal (setEditingDate(null)), recarrega o perfil.
       setDateSaving(true) antes / false depois.

    6. Na lista de datas (dates-strip ou equivalent), modificar cada item para usar d.id como key
       e adicionar botões de ação + badge:
       - key={d.id} em vez de key={d.label} (mais estável)
       - Badge: {d.is_synced && <span className="komi-sync-badge">🎂 sincronizado</span>}
       - Botões: <button onClick={() => handleEditDate(d)}>✏</button>
                 <button onClick={() => handleDeleteDate(d)}>🗑</button>

    7. Renderizar o modal quando editingDate !== null:
       Modal simples com overlay (z-index acima do conteúdo):
         - Input de texto para label (className conforme komi.css existente)
         - DatePicker para date (value={dateFormDate}, onChange={setDateFormDate})
         - Checkbox ou toggle para recurring
         - Botão "Salvar" (chama handleSaveDate) e "Cancelar" (setEditingDate(null))
       Usar classes CSS existentes do komi.css (km-*, btn) — não criar estilos inline
       para elementos que já têm classe.

    Comentários abundantes em português em cada handler e no JSX do modal.
  </action>
  <read_first>
    - webapp/frontend/src/pages/komi/screens/PersonPage.tsx (código completo atual — ver a dates-strip/lista de datas)
    - webapp/frontend/src/components/DatePicker.tsx (após T-04-A — ver props: value, onChange, disabled, placeholder)
    - webapp/frontend/src/pages/komi/komi.css (classes existentes — km-*, btn, btn-primary, btn-ghost, etc.)
    - webapp/frontend/src/pages/komi/types.ts (ImportantDate atualizado na T-04-B)
    - webapp/frontend/src/pages/komi/komiApi.ts (updateDate e deleteDate da T-04-C)
  </read_first>
  <acceptance_criteria>
    - [ ] Cada item de data exibe botões ✏ e 🗑
    - [ ] Clicar ✏ abre modal com label, date e recurring pré-preenchidos com os valores atuais
    - [ ] Alterar date no DatePicker e salvar: PATCH /api/people/{id}/dates/{date_id} é chamado e o perfil recarrega com a data nova
    - [ ] Clicar 🗑 e confirmar: DELETE /api/people/{id}/dates/{date_id} é chamado e a data some da lista
    - [ ] is_synced=true: badge "🎂 sincronizado" aparece ao lado do label da data
    - [ ] is_synced=false: badge não aparece
    - [ ] Compilação TypeScript sem erros (npx tsc --noEmit)
    - [ ] Frontend inicia sem erros no console (npm run dev)
  </acceptance_criteria>
</task>

---

must_haves:
  goal: "Sincronização bidirecional automática de aniversários Komi ↔ Kaguya"
  truths:
    - "birthday_sync_links existe no banco"
    - "criar person_date de aniversário gera tarefa Kaguya"
    - "criar tarefa type=birthday gera person_date Komi"
    - "editar um lado reflete no outro (sem loop)"
    - "agenda mostra aniversário apenas 1x"
    - "KOMI_SYNC_ENABLED=false desliga sync sem quebrar CRUD"
