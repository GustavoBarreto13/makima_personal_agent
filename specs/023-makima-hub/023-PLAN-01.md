---
phase: 23
plan: "023-PLAN-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/frontend/src/pages/makima/types.ts
  - webapp/frontend/src/pages/makima/data.ts
  - webapp/frontend/src/pages/makima/icons.tsx
  - webapp/frontend/src/pages/makima/makima.css
  - webapp/frontend/src/pages/makima/makimaApi.ts
  - webapp/frontend/src/pages/makima/MakimaShell.tsx
  - webapp/frontend/src/App.tsx
  - webapp/frontend/index.html
  - webapp/frontend/public/makima.png
  - webapp/frontend/public/nami.png
  - webapp/frontend/public/frieren.png
  - webapp/frontend/public/komi.png
  - webapp/frontend/public/violet.png
  - webapp/frontend/public/kaguya.jpg
  - webapp/frontend/public/mai.png
  - webapp/frontend/public/marin.png
  - webapp/frontend/public/akane.png
autonomous: true
requirements:
  - REQ-1
  - REQ-2
  - REQ-3
  - REQ-4
  - REQ-5
  - REQ-6
  - REQ-7
  - REQ-8
  - REQ-9
  - REQ-10
  - REQ-15
---

<objective>
Construir o front-end completo do Makima Hub: o Shell em tela cheia (rota `/`, sem `Layout`/sidebar
global), todo o CSS escopado sob `.mkA`, dados estáticos dos 8 agentes, ícones SVG, cliente de API
tipado para `/api/hub/summary`, toggle de tema dark/light persistido, navegação SPA com `<Link>`,
fontes adicionais no `index.html` e os 9 retratos PNG em `public/`. Fidelidade hi-fi ao handoff
"Sala de Controle" (`dirA.jsx` + `README.md`).
</objective>

<tasks>

<task id="T01" type="execute">
<title>Criar types.ts — interfaces do Hub</title>
<read_first>
- specs/023-makima-hub/design_handoff_makima_hub/makima/data.js — formato de cada agente (id, name, role, img, href, does, stat{v,k}, stat2{v,k}, action{label,href,icon}, action2?, accent, accentText)
- specs/023-makima-hub/023-SPEC.md — REQ-11 (formato do endpoint: chave por agente, cada uma `{stat,stat2}` ou `null`) e REQ-15 (fallback null)
- webapp/frontend/src/pages/mai/types.ts — padrão de arquivo types por domínio (para emular estilo)
</read_first>
<action>
Criar `webapp/frontend/src/pages/makima/types.ts` com as interfaces TypeScript do domínio:

- `interface Stat { v: string; k: string }` — valor já formatado (string) + label mono.
- `interface AgentAction { label: string; href: string; icon: string }` — `href` é a rota SPA interna (ex.: `/nami`); `icon` é o nome do ícone em `icons.tsx`.
- `interface Agent { id: string; name: string; role: string; img: string; href: string; does: string; action: AgentAction; action2?: AgentAction; accent: string; accentText: string }` — descreve cada card estático (sem os stats, que vêm da API).
- `interface AgentSummary { stat: Stat; stat2: Stat }` — payload de um agente vindo de `/api/hub/summary`.
- `type HubSummary = Record<string, AgentSummary | null>` — chaves: `nami, frieren, komi, violet, kaguya, mai, marin, akane`; `null` se o agente falhou no backend (REQ-15).
- `interface HeroCopy { kicker: string; role: string; hello: string; lead: string; manifesto: string; tagline: string; footer: string }` — copy do hero.

Comentários em pt-BR explicando o propósito de cada interface (regra global de comentários verbosos).
</action>
<acceptance_criteria>
- Arquivo existe em `webapp/frontend/src/pages/makima/types.ts`.
- Exporta `Stat`, `AgentAction`, `Agent`, `AgentSummary`, `HubSummary`, `HeroCopy`.
- `HubSummary` permite valor `null` por chave (`AgentSummary | null`).
- `npm run build` (após todas as tasks) não acusa erro de tipo em types.ts.
</acceptance_criteria>
</task>

<task id="T02" type="execute">
<title>Criar data.ts — copy do hero + 8 agentes (acentos + rotas SPA)</title>
<read_first>
- specs/023-makima-hub/design_handoff_makima_hub/makima/data.js — `window.MAKIMA.copy` e `window.MAKIMA.agents` (texto exato de does/role/labels/icons + accent/accentText)
- specs/023-makima-hub/023-SPEC.md — REQ-5 (valores OKLCH exatos) e REQ-10 (mapa de rotas SPA: Nami→/nami, Frieren→/books, Komi→/people, Violet→/journal, Kaguya→/tasks, Mai→/series, Marin→/animes, Akane→/movies; Kaguya action2 "Abrir agenda"→/tasks)
- webapp/frontend/src/pages/makima/types.ts — interfaces criadas em T01
</read_first>
<action>
Criar `webapp/frontend/src/pages/makima/data.ts` exportando:

1. `export const HERO_COPY: HeroCopy` com os textos EXATOS do `data.js`:
   - kicker: "CENTRO DE CONTROLE"
   - role: "Orquestradora"
   - hello: "Bom te ver de volta."
   - lead: "Cada parte da sua vida tem uma responsável."
   - manifesto: "Dinheiro, leituras, pessoas, tempo, histórias — nada fica solto, nada se perde. Cada agente cuida do que importa no seu domínio. Eu mantenho todas em ordem para que você só precise decidir o que de fato importa."
   - tagline: "Oito agentes cuidam de tudo. Eu cuido delas."
   - footer: "Tudo sob controle."

2. `export const MAKIMA_IMG = '/makima.png'`.

3. `export const AGENTS: Agent[]` — array de 8 na ordem Nami→Frieren→Komi→Violet→Kaguya→Mai→Marin→Akane. Para cada agente, copiar `name`, `role`, `does`, `action.label`, `action.icon` do `data.js`, MAS substituir:
   - `img`: caminho absoluto da public (`/nami.png`, `/frieren.png`, `/komi.png`, `/violet.png`, `/kaguya.jpg`, `/mai.png`, `/marin.png`, `/akane.png`). Nota: Kaguya usa `.jpg`.
   - `href` e `action.href`: a rota SPA interna (NÃO os `.html` do protótipo). Mapa REQ-10:
     - nami → `/nami`; frieren → `/books`; komi → `/people`; violet → `/journal`; kaguya → `/tasks`; mai → `/series`; marin → `/animes`; akane → `/movies`.
   - Kaguya recebe `action2: { label: 'Abrir agenda', href: '/tasks', icon: 'calendar' }`.
   - `accent` / `accentText`: os valores OKLCH EXATOS do SPEC REQ-5 / tabela do handoff:
     - nami: `oklch(0.74 0.168 57)` / `oklch(0.80 0.150 62)`
     - frieren: `oklch(0.77 0.118 184)` / `oklch(0.82 0.110 184)`
     - komi: `oklch(0.70 0.135 276)` / `oklch(0.78 0.125 276)`
     - violet: `oklch(0.70 0.088 253)` / `oklch(0.78 0.090 253)`
     - kaguya: `oklch(0.72 0.165 340)` / `oklch(0.80 0.150 340)`
     - mai: `oklch(0.70 0.120 292)` / `oklch(0.78 0.115 292)`
     - marin: `oklch(0.74 0.16 210)` / `oklch(0.84 0.14 208)`
     - akane: `oklch(0.66 0.115 196)` / `oklch(0.79 0.10 192)`
   - `icon` por agente: nami→plus, frieren→book, komi→user, violet→pen, kaguya→check, mai→tv, marin→sparkle, akane→film.

Não incluir `stat`/`stat2` em `AGENTS` — esses vêm da API (T05/T06).
</action>
<acceptance_criteria>
- Arquivo existe em `webapp/frontend/src/pages/makima/data.ts`.
- `AGENTS.length === 8` e a ordem dos `id` é exatamente `nami,frieren,komi,violet,kaguya,mai,marin,akane`.
- Nenhum `href` em data.ts termina em `.html` (grep por `.html` retorna 0 ocorrências no arquivo).
- `AGENTS.find(a => a.id==='kaguya').action2.href === '/tasks'`.
- Os 8 pares accent/accentText batem caractere-a-caractere com a tabela REQ-5 (inspeção textual).
- `AGENTS.find(a => a.id==='kaguya').img` termina em `.jpg`; os outros 7 em `.png`.
</acceptance_criteria>
</task>

<task id="T03" type="execute">
<title>Criar icons.tsx — componente MkIcon em React/TSX</title>
<read_first>
- specs/023-makima-hub/design_handoff_makima_hub/makima/icons.jsx — mapa de paths `P` e a fábrica MkIcon (stroke 1.7, viewBox 0 0 24 24)
- webapp/frontend/src/pages/mai/components/MaiIcons.tsx — padrão de ícones SVG inline em TSX no codebase (para emular tipagem de props)
</read_first>
<action>
Criar `webapp/frontend/src/pages/makima/icons.tsx` portando o `icons.jsx` para JSX/TSX real (não `React.createElement`):

- Constante `const PATHS: Record<string, string>` com os mesmos pares nome→path do `icons.jsx`: `plus, book, user, pen, check, calendar, tv, sparkle, film, arrow, arrowUR, grid, chev, moon` (copiar os `d` exatos).
- `interface MkIconProps { name: string; size?: number; style?: React.CSSProperties; className?: string }`.
- `export function MkIcon({ name, size = 18, style, className }: MkIconProps)` que renderiza um `<svg>` com `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth={1.7}`, `strokeLinecap="round"`, `strokeLinejoin="round"`, `aria-hidden`, e um `<path d={PATHS[name] || PATHS.arrow} />`.

`MkSpiral` é opcional (não usado na direção A) — pode ser omitido.
Comentários pt-BR explicando que os paths vêm do handoff e que `currentColor` herda a cor do contexto.
</action>
<acceptance_criteria>
- Arquivo existe em `webapp/frontend/src/pages/makima/icons.tsx`.
- Exporta `MkIcon`.
- `PATHS` contém ao menos as chaves usadas em data.ts: `plus, book, user, pen, check, tv, sparkle, film, calendar` e ainda `arrowUR` e `moon`.
- O SVG renderizado usa `strokeWidth={1.7}` e `stroke="currentColor"` (inspeção do código).
- Sem uso de `React.createElement` (usa JSX).
</acceptance_criteria>
</task>

<task id="T04" type="execute">
<title>Criar makima.css — 100% escopado sob .mkA (tokens, hero, roster, temas, responsivo)</title>
<read_first>
- specs/023-makima-hub/design_handoff_makima_hub/makima/dirA.jsx — o bloco `CSS` (linhas 7–139): copiar regra a regra
- specs/023-makima-hub/023-SPEC.md — REQ-2 (escopo), REQ-3 (hero), REQ-4 (cards), REQ-6 (temas), REQ-7 (breakpoints)
- C:\Users\gusta\.claude\projects\...\memory css-shell-leakage (princípio) e webapp/frontend/src/pages/mai/mai.css — padrão de scoping por shell no codebase
</read_first>
<action>
Criar `webapp/frontend/src/pages/makima/makima.css` portando integralmente o CSS do `dirA.jsx`.
Toda regra DEVE começar com `.mkA` (ou `.mkA[data-theme=...]` / `.mkA[data-red=...]` / `.mkA[data-cols=...]`).
Não criar nenhuma regra global (`.hero`, `.card`, `body`, `html`, `*` soltos fora de `.mkA`).

Conteúdo obrigatório (copiar exatamente do handoff):
- Bloco `.mkA{...}`: todos os tokens dark — `--bg, --bg2, --panel, --line, --red, --red-br, --gold, --ink, --ink2, --ink3, --serif, --sans, --mono, --glow:0.30`; os dois `radial-gradient` de fundo + `var(--bg)`; `color`, `font-family`, `position:relative`, `overflow:hidden`.
- `.mkA *{box-sizing:border-box;}` e `.mkA::before` (scanlines).
- `.mkA .wrap` (max-width 1000px, padding 0 56px).
- Topbar: `.mkA .top`, `.top .b`, `.top .dot`, `.top .topr`, `.themetog`, `.themetog:hover`.
- Hero: `.mkA .hero` (grid 1fr 360px), `.kick`, `.kick::before`, `.h1` (font-size **104px**, weight 800, line-height 0.92), `.h1 em` (italic, `var(--red-br)`), `.role`, `.hello`, `.manifesto`, `.tagline`, `.tagline b`, `.meta`, `.meta .n`, `.meta .l`.
- Retrato: `.portrait`, `.portrait .halo`, `.portrait .ring`, `.portrait .ring.r2` (anel vermelho tracejado), `.portrait img`.
- Section label: `.seclbl`, `.seclbl .t`, `.seclbl .num`, `.seclbl .rule`.
- Roster: `.roster` (grid 1fr 1fr), `.card`, `.card::after` (barra de acento `var(--ac)` + glow), `.card:hover` (`translateY(-3px)` + borda `var(--ac)` + sombra), `.card .body` (width 64%), `.idx`, `.nm` (serif 30px), `.rl` (`var(--ac-t)`), `.ds`, `.stats`, `.stat`, `.stat .v`, `.stat .v b` (`var(--ac-t)`), `.stat .k`, `.acts`, `.btn`, `.btn:active`, `.btn.primary` (`background:var(--ac)`), `.btn.primary:hover`, `.btn.ghost`, `.btn.ghost:hover`, `.card .ph` (máscara `mask-image: linear-gradient(90deg, transparent, #000 26%)` + `-webkit-mask-image`), `.card .ph .pg` (glow radial `var(--ac)`), `.card .ph img`, `.card .open`, `.card:hover .open`.
- Footer: `.foot`, `.foot .r`.
- Variantes: `.mkA[data-red="sobrio"]` (copiar) e `.mkA[data-cols="1"] .roster` (copiar) — mantidos para fidelidade, ainda que os tweaks não tenham UI nesta fase.
- Tema claro: bloco `.mkA[data-theme="light"]{...}` (todos os tokens light incl. `--glow:0.16`), `.mkA[data-theme="light"]::before{display:none;}`, e as 6 sobras (`.card`, `.card:hover`, `.card .rl`, `.card .stat .v b`, `.card .open`, `.portrait img`).
- Responsivo: `@media (max-width:1040px)` (`.wrap` padding 40px, `.h1` **88px**); `@media (max-width:900px)` (hero 1 coluna, `.portrait{order:-1;height:300px}`, `.portrait img` 300px, rings 230/262, `.h1` **80px**); `@media (max-width:680px)` (`.wrap` padding 22px, `.h1` **56px**, `.roster` 1 coluna, etc.).
</action>
<acceptance_criteria>
- Arquivo existe em `webapp/frontend/src/pages/makima/makima.css`.
- A primeira regra do arquivo inicia com `.mkA` (após comentários iniciais).
- Grep no arquivo NÃO encontra nenhuma linha de seletor que comece com `.hero`, `.card`, `.roster`, `.h1`, `.portrait`, `.top` ou `.btn` sem o prefixo `.mkA ` antes (todas devem ser `.mkA .hero`, `.mkA .card`, etc.).
- Grep NÃO encontra regras `body{`, `html{`, ou `*{` no nível global (apenas `.mkA *{`).
- A regra `.mkA .h1` tem `font-size:104px`.
- `@media (max-width:680px)` define `.mkA .h1{font-size:56px}` e `.mkA .roster{grid-template-columns:1fr}`.
- `@media (max-width:1040px)` define `.mkA .h1{font-size:88px}`.
- `.mkA .card .ph` contém `mask-image:linear-gradient(90deg,transparent,#000 26%)`.
- `.mkA[data-theme="light"]` existe com `--glow:0.16`.
</acceptance_criteria>
</task>

<task id="T05" type="execute">
<title>Criar makimaApi.ts — cliente tipado de /api/hub/summary</title>
<read_first>
- webapp/frontend/src/lib/api.ts — `api.get<T>(path)` (envia cookie, lança Error em não-2xx)
- webapp/frontend/src/pages/nami/namiApi.ts — padrão de objeto API por domínio (estrutura `export const xApi = { ... }`)
- webapp/frontend/src/pages/makima/types.ts — `HubSummary`
</read_first>
<action>
Criar `webapp/frontend/src/pages/makima/makimaApi.ts`:

- `import { api } from '../../lib/api'`
- `import type { HubSummary } from './types'`
- `export const makimaApi = { getSummary: () => api.get<HubSummary>('/api/hub/summary') }`

Componentes NÃO podem chamar `fetch` nem `api.get` diretamente — só `makimaApi.getSummary()` (regra do webapp/CLAUDE.md). Comentários pt-BR.
</action>
<acceptance_criteria>
- Arquivo existe em `webapp/frontend/src/pages/makima/makimaApi.ts`.
- Exporta `makimaApi` com método `getSummary`.
- `getSummary` chama `api.get<HubSummary>('/api/hub/summary')` (string de path exata).
- Nenhuma chamada a `fetch(` no arquivo.
</acceptance_criteria>
</task>

<task id="T06" type="execute">
<title>Criar MakimaShell.tsx — Shell em tela cheia, tema persistido, navegação SPA, fallback null</title>
<read_first>
- specs/023-makima-hub/design_handoff_makima_hub/makima/dirA.jsx — estrutura JSX do `DirA` (topbar, hero, seclbl, roster, foot)
- webapp/frontend/src/pages/mai/MaiShell.tsx — padrão de Shell (import do css no topo, persistência de tweaks em localStorage com try/catch, useEffect de fetch no mount)
- webapp/frontend/src/pages/makima/{data,types,icons,makimaApi}.ts(x) — dependências criadas
- specs/023-makima-hub/023-SPEC.md — REQ-1 (sem Layout), REQ-6 (tema/localStorage `makima-hub-theme`), REQ-10 (Link), REQ-15 (fallback "—")
</read_first>
<action>
Criar `webapp/frontend/src/pages/makima/MakimaShell.tsx`:

Imports: `import { useState, useEffect } from 'react'`; `import { Link } from 'react-router-dom'`; `import { AGENTS, HERO_COPY, MAKIMA_IMG } from './data'`; `import { MkIcon } from './icons'`; `import { makimaApi } from './makimaApi'`; `import type { HubSummary } from './types'`; `import './makima.css'`.

Constante: `const THEME_KEY = 'makima-hub-theme'` (REQ-6).

Helper `loadTheme(): 'dark' | 'light'` — lê `localStorage.getItem(THEME_KEY)`; retorna `'light'` só se for exatamente `'light'`, senão `'dark'` (default dark). Envolver em try/catch (localStorage pode falhar).

Componente `export function MakimaShell()`:
- `const [theme, setTheme] = useState<'dark'|'light'>(loadTheme)`.
- `const [summary, setSummary] = useState<HubSummary | null>(null)` — null = ainda carregando.
- `useEffect` no mount: `makimaApi.getSummary().then(setSummary).catch(() => setSummary({}))` — em erro total, objeto vazio para que todos os cards caiam no fallback "—".
- `useEffect([theme])`: persiste `localStorage.setItem(THEME_KEY, theme)` em try/catch.
- Toggle: `function toggleTheme() { setTheme(t => t === 'dark' ? 'light' : 'dark') }`.
- Helper `statText(agentId: string, which: 'stat'|'stat2', field: 'v'|'k'): string` que lê `summary?.[agentId]?.[which]?.[field]`; se `summary` ainda null (carregando) OU o agente é `null` OU campo ausente → retorna `'—'` para `v` e o label fixo de fallback para `k` (ver abaixo). REQ-15.
  - Para `k` (label), preferir o valor da API quando presente; quando ausente/carregando, exibir `'—'` também (label some junto). Simplificação aceitável: no estado de loading e no fallback null, ambos `v` e `k` mostram `'—'`.

Render — replicar a árvore do `DirA`, com escopo `.mkA` e `data-theme`:
- `<div className="mkA" data-theme={theme}>` → `<div className="wrap">`.
- Topbar: `.top` com `.b` (dot + `HERO_COPY.kicker`) e `.topr` (`Hub · {AGENTS.length} agentes · 9 domínios` + `<button className="themetog" onClick={toggleTheme}><MkIcon name="moon" size={13}/> Tema</button>`).
- Hero: coluna de texto (`.kick` = `HERO_COPY.role`; `<h1 className="h1">Mak<em>i</em>ma</h1>`; `.role` = `{HERO_COPY.role} · Sistema de Vida`; `.hello`; `.manifesto`; `.tagline` com `<b>{HERO_COPY.lead}</b> {HERO_COPY.tagline}`; `.meta` com 8 Agentes / 9 Domínios / 1 No comando). Coluna `.portrait` com `.halo`, `.ring.r2`, `.ring`, `<img src={MAKIMA_IMG} alt="Makima"/>`.
- `.seclbl` (Os domínios + rule + `/ 09`).
- `.roster`: `AGENTS.map((a, i) => ...)` renderizando cada `.card` com `style={{ '--ac': a.accent, '--ac-t': a.accentText } as React.CSSProperties}`:
  - `<Link className="open" to={a.href} title={'Abrir '+a.name}><MkIcon name="arrowUR" size={14}/></Link>`.
  - `.body`: `.idx` = `String(i+1).padStart(2,'0')`; `.nm` = a.name; `.rl` = a.role; `.ds` = a.does.
  - `.stats`: dois `.stat`. Primeiro: `<div className="v"><b>{statText(a.id,'stat','v')}</b></div><div className="k">{statText(a.id,'stat','k')}</div>`. Segundo análogo com `stat2`.
  - `.acts`: `<Link className="btn primary" to={a.action.href}><MkIcon name={a.action.icon} size={15}/>{a.action.label}</Link>` e, se `a.action2`, `<Link className="btn ghost" to={a.action2.href}>...`.
  - `.ph`: `.pg` + `<img src={a.img} alt={a.name} style={{ objectPosition: 'bottom' }}/>`.
- `.foot`: span esquerdo "Makima · Centro de Controle" + `<span className="r">{HERO_COPY.footer}</span>`.

NÃO usar `<a href>` para navegação interna; NÃO envolver em `<Layout>`. O wrapper raiz é `.mkA`.
Comentários pt-BR.
</action>
<acceptance_criteria>
- Arquivo existe em `webapp/frontend/src/pages/makima/MakimaShell.tsx` e exporta `MakimaShell`.
- Importa `./makima.css` no topo.
- Não há `<a ` (anchor tag) no JSX; navegação usa `<Link to=...>` (grep por `<a ` retorna 0; grep por `<Link` retorna ≥ 10).
- O elemento raiz é `<div className="mkA" data-theme={theme}>` e NÃO há import nem uso de `Layout`.
- `THEME_KEY` é a string `'makima-hub-theme'`.
- O `<h1>` contém `Mak<em>i</em>ma` (o "i" em `<em>`).
- Cada card define `--ac` e `--ac-t` via `style` a partir de `a.accent`/`a.accentText`.
- Quando `summary` é `null` ou `summary[id]` é `null`, os stats renderizam `'—'` sem lançar erro (verificável por code review do `statText`).
</acceptance_criteria>
</task>

<task id="T07" type="execute">
<title>Registrar rota / em App.tsx e remover Dashboard de /</title>
<read_first>
- webapp/frontend/src/App.tsx — bloco de Routes; especialmente a `<Route path="/*" element={<Layout>...}` e a `<Route path="/" element={<Dashboard />}>` interna
- webapp/frontend/src/pages/makima/MakimaShell.tsx — componente a importar
- specs/023-makima-hub/023-SPEC.md — REQ-1 (rota `/` antes do catch-all; remover Dashboard de `/`)
</read_first>
<action>
Editar `webapp/frontend/src/App.tsx`:

1. Adicionar import: `import { MakimaShell } from './pages/makima/MakimaShell'`.
2. Adicionar, DENTRO do `<Routes>` externo e ANTES do `<Route path="/*" element={<Layout>...`, a rota: `<Route path="/" element={<MakimaShell />} />`. Ela deve vir junto dos outros Shells (antes do catch-all), pois é uma rota exata `/`.
3. Remover a linha interna `<Route path="/" element={<Dashboard />} />` (dentro do `<Routes>` aninhado do Layout) — agora `/` é o Hub, não o Dashboard. As demais rotas do Layout (`/transactions`, `/accounts`, etc.) permanecem.
4. Se o import `Dashboard` ficar sem uso após a remoção, remover também `import Dashboard from './pages/Dashboard'`.

Não tocar nas rotas dos outros Shells.
</action>
<acceptance_criteria>
- `App.tsx` contém `import { MakimaShell } from './pages/makima/MakimaShell'`.
- `App.tsx` contém `<Route path="/" element={<MakimaShell />} />` posicionada antes de `<Route path="/*"`.
- O `<Routes>` aninhado dentro do `<Layout>` NÃO contém mais `element={<Dashboard />}`.
- Se Dashboard não é mais referenciado, o import de Dashboard foi removido (grep por `Dashboard` em App.tsx retorna 0).
- `npm run build` compila sem erro de "Dashboard is declared but never used".
- As rotas `/transactions`, `/accounts`, `/cards`, `/loans`, `/budgets`, `/subscriptions` continuam dentro do Layout.
</acceptance_criteria>
</task>

<task id="T08" type="execute">
<title>Adicionar pesos de fonte no index.html (Playfair 800, DM Sans 600/700)</title>
<read_first>
- webapp/frontend/index.html — a tag `<link>` do Google Fonts (linha 14)
- specs/023-makima-hub/023-SPEC.md — REQ-8 (string de fonte alvo exata)
</read_first>
<action>
Editar `webapp/frontend/index.html`: na URL do Google Fonts (`fonts.googleapis.com/css2?...`), atualizar os segmentos de família para incluir os novos pesos, preservando os demais (DM Mono e Newsreader intactos):

- Trocar `family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400` por `family=Playfair+Display:ital,wght@0,400;0,600;0,700;0,800;1,400` (adiciona o peso 800).
- Trocar `family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300;1,400` por `family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400` (adiciona 600 e 700).
- Manter `&display=swap` e a ordem dos `&family=` existentes.

Não remover Newsreader nem DM Mono.
</action>
<acceptance_criteria>
- A URL do Google Fonts em index.html contém `Playfair+Display:ital,wght@0,400;0,600;0,700;0,800;1,400`.
- A URL contém `DM+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400`.
- A URL ainda contém `DM+Mono` e `Newsreader` (não foram removidos).
- A URL termina com `&display=swap`.
</acceptance_criteria>
</task>

<task id="T09" type="execute">
<title>Copiar 9 retratos PNG/JPG do handoff para public/</title>
<read_first>
- specs/023-makima-hub/design_handoff_makima_hub/assets/ — origem (já confirmado: contém makima.png, nami.png, frieren.png, komi.png, violet.png, kaguya.jpg, mai.png, marin.png, akane.png)
- webapp/frontend/public/ — destino (hoje faltam makima.png, nami.png, akane.png; tem nami.jpg legado)
- specs/023-makima-hub/023-SPEC.md — REQ-9 (9 assets; referências `/makima.png` etc.; kaguya mantém .jpg)
</read_first>
<action>
Copiar os 9 arquivos de `specs/023-makima-hub/design_handoff_makima_hub/assets/` para `webapp/frontend/public/`, sobrescrevendo os existentes para garantir as versões transparentes do handoff:
`makima.png, nami.png, frieren.png, komi.png, violet.png, kaguya.jpg, mai.png, marin.png, akane.png`.

Usar cópia binária (ex.: `cp` no Bash tool). NÃO copiar para `frontend/dist/` (é build). `kaguya.jpg` mantém extensão `.jpg`. O `nami.jpg`/`nami-hero.png` legados podem permanecer (não atrapalham), mas `nami.png` agora deve existir e ser o referenciado por data.ts.
</action>
<acceptance_criteria>
- Os 9 arquivos existem em `webapp/frontend/public/`: makima.png, nami.png, frieren.png, komi.png, violet.png, kaguya.jpg, mai.png, marin.png, akane.png.
- `webapp/frontend/public/makima.png` e `webapp/frontend/public/akane.png` existem (estavam faltando).
- `webapp/frontend/public/nami.png` existe (estava faltando).
- Cada arquivo tem tamanho > 0 bytes.
- Nenhum arquivo foi criado em `webapp/frontend/dist/`.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Rodar `cd webapp/frontend && npm run build` — deve sair com código 0, sem erros de TypeScript (REQ acceptance "npm run build completa sem erros").
2. Rodar `npm run dev` (ou servir o build), abrir `/`:
   - Hub aparece em tela cheia, SEM sidebar de domínios (`.mkA` presente, nenhum `<aside>` da Layout).
   - h1 "Makima" com "i" vermelho em itálico; a ≥1041px de largura, `getComputedStyle(h1).fontSize === '104px'`.
   - 8 cards na ordem Nami→Frieren→Komi→Violet→Kaguya→Mai→Marin→Akane; cada `--ac` no DevTools bate com a tabela REQ-5.
   - Hover em um card aplica `translateY(-3px)` (Computed → transform).
3. Tema: clicar "Tema" alterna `data-theme` entre dark/light; recarregar mantém o último (localStorage `makima-hub-theme`).
4. Responsivo: viewport 680px → roster 1 coluna e h1 56px; 900px → hero 1 coluna (retrato acima); 1040px → h1 88px.
5. Navegação: clicar "Adicionar transação" navega para `/nami` sem reload (Network sem novo documento HTML); clicar ↗ da Frieren vai para `/books`; Kaguya "Abrir agenda" vai para `/tasks`.
6. Navegar para `/transactions` → a sidebar global (Layout) reaparece normalmente (REQ-1).
7. Assets: `GET /makima.png` e `GET /akane.png` retornam 200; imagens renderizam transparentes.
8. Isolamento CSS: abrir `/series` (MaiShell) após visitar `/` — nenhuma regra `.mkA` ativa fora do wrapper (DevTools); layout do Mai inalterado.
9. Fallback: se a API retornar `null` para um agente (ver PLAN-02 / mock), o card mostra "—" nos dois stats sem erro no console.
</verification>

<must_haves>
<truths>
- A rota `/` renderiza `MakimaShell` em tela cheia, sem o `Layout`/sidebar global; `/transactions` continua dentro do Layout com sidebar.
- Todo o `makima.css` está escopado sob `.mkA`; visitar o Hub não altera visualmente nenhum outro shell (sem vazamento CSS).
- O hero é fiel ao handoff: h1 104px serif 800 com "i" vermelho itálico, halo + anel amarelo + anel vermelho tracejado.
- Os 8 cards têm os acentos OKLCH exatos do SPEC e o retrato flutuante com máscara de fade + glow.
- Tema dark/light alterna pela topbar e persiste em `localStorage['makima-hub-theme']` (default dark).
- Toda navegação dos cards usa `<Link>` (SPA, sem reload) e aponta para as rotas reais dos agentes.
- Os 9 retratos existem em `public/` e são servidos por caminho absoluto (`/makima.png`, etc.).
- Stats nulos/em carregamento exibem "—" sem quebrar o card (fallback gracioso).
- `npm run build` compila sem erros de TypeScript.
</truths>
</must_haves>
